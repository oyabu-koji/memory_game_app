setup-project を実行して、このプロジェクトの `docs/` を初期化してください。

前提:
- `AGENTS.md` のルールに従う
- `docs/ideas/initial-requirements.md` を出発点にする
- `docs/` の6文書を作成するまで実装に進まない
- docs 作成後に `.steering/[今日の日付]-initial-setup/` を必要なら作成する
